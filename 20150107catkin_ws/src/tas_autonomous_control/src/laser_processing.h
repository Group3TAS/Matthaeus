#ifndef LASER_PROCESSING_H_GUARD
#define LASER_PROCESSING_H_GUARD

#include "laser_processing.h"
#include <vector>

float process_laser_data(std::vector<float>);
float min_right(std::vector<float>&);
float min_left(std::vector<float>&);
#endif
